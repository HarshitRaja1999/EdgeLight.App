<Window x:Class="EdgeLight.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EdgeLight.App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <!-- Accent + base brushes -->
        <SolidColorBrush x:Key="AccentBrush" Color="#4C7DFF" />
        <SolidColorBrush x:Key="AccentBrushSoft" Color="#334C7DFF" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#1A1A1A" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F5F5F5" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A0A0A0" />

        <!-- Text styles -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="SectionHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,16,0,4" />
        </Style>

        <!-- Button style -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#66000000"
                                      BlurRadius="12"
                                      ShadowDepth="2" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrushSoft}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2A4C7DFF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox style (minimal, Apple-y) -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <!-- Slider style: keep default template, just tweak look -->
        <Style TargetType="Slider">
            <!-- Just some spacing and accent -->
            <Setter Property="Margin" Value="0,4,0,0" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
        </Style>



    </Window.Resources>

    <!-- Centered card -->
    <Grid>
        <Border Background="{StaticResource PanelBackgroundBrush}"
                CornerRadius="18"
                Padding="20"
                Width="380"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#AA000000"
                                  BlurRadius="22"
                                  ShadowDepth="4" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Edge Light"
                                   Style="{StaticResource TitleTextStyle}" />
                        <TextBlock Text="Desktop soft light controller"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Toggle row -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,0" VerticalAlignment="Center">
                    <CheckBox x:Name="LightEnabledCheckBox"
                              Content="Enable edge light"
                              IsChecked="True"
                              Checked="LightEnabledChanged"
                              Unchecked="LightEnabledChanged" />
                </StackPanel>

                <!-- Sliders -->
                <StackPanel Grid.Row="2" Margin="0,8,0,0">

                    <TextBlock Text="Color (Cool → Warm)"
                               Style="{StaticResource SectionHeaderTextStyle}" />
                    <Slider x:Name="ColorSlider"
                            Value="50"
                            TickFrequency="10"
                            IsSnapToTickEnabled="False"
                            ValueChanged="ColorSlider_ValueChanged" />

                    <TextBlock Text="Intensity"
                               Style="{StaticResource SectionHeaderTextStyle}" />
                    <Slider x:Name="IntensitySlider"
                            Value="50"
                            TickFrequency="10"
                            IsSnapToTickEnabled="False"
                            ValueChanged="IntensitySlider_ValueChanged" />

                    <TextBlock Text="Edge Softness (Blurred ←→ Crisp)"
                               Style="{StaticResource SectionHeaderTextStyle}" />
                    <Slider x:Name="CrispnessSlider"
                            Value="50"
                            TickFrequency="10"
                            IsSnapToTickEnabled="False"
                            ValueChanged="CrispnessSlider_ValueChanged" />

                </StackPanel>

                <!-- Footer / Bottom row -->
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">

                    <Button Content="Exit"
                            Width="90"
                            Height="30"
                            Click="Exit_Click" />
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Window>
